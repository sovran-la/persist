[package]
name = "persist-android-test"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
persist = { path = "../../.." }
jni = { version = "0.21", default-features = false }
